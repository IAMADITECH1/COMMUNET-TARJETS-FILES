const API_URL='https://script.google.com/macros/s/AKfycbxtffspuIEkJKnGCHBpFbLwNdp6zIoEsp8pvB3nXBosAoz03IvjItDvC50D6cK08TEb/exec';const TOTAL_STAMPS=10;const folio=new URLSearchParams(location.search).get('folio');const lockedView=document.querySelector('#lockedView');const cardView=document.querySelector('#cardView');const lockedTitle=document.querySelector('#lockedTitle');const lockedMessage=document.querySelector('#lockedMessage');
function lock(t,m){lockedTitle.textContent=t;lockedMessage.textContent=m;lockedView.hidden=false;cardView.hidden=true;}function render(c){document.querySelector('#clientName').textContent=c.name;document.querySelector('#clientFolio').textContent=c.folio;document.querySelector('#stamps').innerHTML=Array.from({length:TOTAL_STAMPS},(_,i)=>{const f=i<c.visits;return `<span class="overlay-stamp ${f?'filled':''}" aria-label="Sello ${i+1}">${f?'♥':'♡'}</span>`;}).join('');cardView.hidden=false;lockedView.hidden=true;}
function lookup(){const cb=`bichaSecureLookup_${Date.now()}`,s=document.createElement('script'),timeout=setTimeout(()=>{clean();lock('No se pudo consultar la tarjeta','Intenta nuevamente en unos segundos.');},8000);function clean(){clearTimeout(timeout);delete window[cb];s.remove();}window[cb]=(r)=>{clean();if(r?.ok&&r.found&&Number(r.visits)>0)render({name:r.name,folio:r.folio,visits:Number(r.visits)});else lock('Tarjeta pendiente de activación','Tu registro está guardado, pero la administradora todavía no ha registrado una visita.');};s.onerror=()=>{clean();lock('No se pudo consultar la tarjeta','Intenta nuevamente en unos segundos.');};s.src=`${API_URL}?action=client&folio=${encodeURIComponent(folio)}&callback=${cb}`;document.head.appendChild(s);}
if(!folio)lock('Necesitas un enlace personal','Esta tarjeta solo puede abrirse desde el QR asignado a la clienta.');else lookup();
